*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
/app/build/

# ─── Modelo de lenguaje local ───
# El fichero .task (cientos de MB) NO se versiona: su licencia exige
# aceptación individual por parte de quien lo descarga, y su tamaño
# desaconseja incluirlo en el repositorio. Ver assets/LEEME_MODELO.txt.
*.task

# ─── Firma de la aplicación ───
# Nunca subir el almacén de claves ni sus contraseñas.
*.jks
*.keystore
keystore.properties
signing.properties

# ─── Herramientas de asistencia y notas de trabajo ───
# El historial se mantiene limpio de atribuciones a herramientas: estos
# archivos son de uso local y no forman parte de la entrega.
CLAUDE.md
INFORME_COHERENCIA.md

# ─── Otros ───
*.apk
*.aab
.kotlin/
